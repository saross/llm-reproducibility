{
  "permissions": {
    "allow": [
      // ============================================
      // CORE FILE OPERATIONS
      // ============================================
      "Read",
      "Edit",
      "Write",
      
      // ============================================
      // WEB ACCESS
      // ============================================
      "WebSearch",
      "WebFetch",
      
      // ============================================
      // FILE SYSTEM READ PERMISSIONS
      // ============================================
      // Project files
      "Read(/home/shawn/Code/llm-reproducibility/**)",
      // Claude configuration
      "Read(/home/shawn/.claude/**)",
      
      // ============================================
      // NAVIGATION & FILE VIEWING
      // ============================================
      "Bash(cd *)",           // Change directory
      "Bash(ls *)",           // List files
      "Bash(tree *)",         // Directory tree view
      "Bash(cat *)",          // View file contents
      "Bash(head *)",         // View file headers
      "Bash(tail *)",         // View file tail
      "Bash(less *)",         // Page through files
      "Bash(find *)",         // Find files
      "Bash(wc *)",           // Count lines/words
      
      // ============================================
      // FILE OPERATIONS
      // ============================================
      "Bash(cp *)",           // Copy files
      "Bash(mv *)",           // Move/rename files
      "Bash(mkdir *)",        // Create directories
      "Bash(chmod *)",        // Change permissions
      "Bash(unzip *)",        // Extract archives
      "Bash(tar *)",          // Archive operations
      "Bash(touch *)",        // Create/update files
      "Bash(rm *)",           // Remove files (but rm -rf blocked in deny)
      
      // ============================================
      // TEXT PROCESSING
      // ============================================
      "Bash(echo *)",         // Output text
      "Bash(awk *)",          // Text processing
      "Bash(sed *)",          // Stream editor
      "Bash(grep *)",         // Search text
      "Bash(xargs *)",        // Build commands from input
      "Bash(sort *)",         // Sort lines
      "Bash(uniq *)",         // Remove duplicates
      "Bash(cut *)",          // Cut columns
      "Bash(paste *)",        // Merge lines
      "Bash(tr *)",           // Translate characters
      
      // ============================================
      // DATA PROCESSING
      // ============================================
      "Bash(jq *)",           // JSON processing
      
      // ============================================
      // PYTHON & PACKAGE MANAGEMENT
      // ============================================
      // Python execution
      "Bash(python3 *)",      // Direct Python execution
      "Bash(python3 << *)",   // Python heredocs
      "Bash(python *)",       // Python (any version)
      
      // Virtual environment
      "Bash(python3 -m venv *)",
      "Bash(./venv/bin/python *)",
      "Bash(source venv/bin/activate)",
      "Bash(. venv/bin/activate)",
      
      // Package installation
      "Bash(pip install *)",
      "Bash(pip list *)",
      "Bash(pip show *)",
      "Bash(./venv/bin/pip *)",
      
      // ============================================
      // SCRIPTS & EXECUTABLES
      // ============================================
      "Bash(./*.sh *)",       // Execute shell scripts in current directory
      "Bash(./extraction-system/*.sh *)",  // Project-specific scripts
      
      // ============================================
      // VERSION CONTROL (GIT)
      // ============================================
      "Bash(git *)",          // All git operations (except force push - see deny list)
      
      // ============================================
      // BASH CONTROL STRUCTURES & UTILITIES
      // ============================================
      // Control flow
      "Bash(for * in *)",     // For loops
      "Bash(do)",             // Loop/conditional body start
      "Bash(done)",           // Loop end
      "Bash(if *)",           // Conditional start
      "Bash(then)",           // Conditional body
      "Bash(fi)",             // Conditional end
      "Bash(else)",           // Else clause
      "Bash(elif *)",         // Elif clause
      "Bash(while *)",        // While loops
      "Bash(until *)",        // Until loops
      "Bash(case *)",         // Case statements
      "Bash(esac)",           // Case end
      
      // Testing & comparison
      "Bash(test *)",         // Test conditions
      "Bash([ * ])",          // Test syntax
      "Bash([[ * ]])",        // Extended test
      
      // Variable operations
      "Bash(export *)",       // Export variables
      "Bash(unset *)",        // Unset variables
      "Bash(declare *)",      // Declare variables
      "Bash(local *)",        // Local variables
      
      // Redirection & pipes
      "Bash(* > *)",          // Output redirection
      "Bash(* >> *)",         // Append redirection
      "Bash(* | *)",          // Pipe
      "Bash(* 2>&1)",         // Error redirection
      "Bash(* > /dev/null)",  // Null redirect
      "Bash(* && *)",         // AND chaining
      "Bash(* || *)",         // OR chaining
      "Bash(* ; *)",          // Sequential execution
      
      // Process management
      "Bash(sleep *)",        // Sleep/pause
      "Bash(wait *)",         // Wait for process
      "Bash(ps *)",           // List processes
      "Bash(kill *)",         // Kill processes (for Claude's own processes)
      
      // String operations
      "Bash(printf *)",       // Formatted output
      "Bash(basename *)",     // Get filename
      "Bash(dirname *)",      // Get directory
      "Bash(realpath *)",     // Get absolute path
      
      // ============================================
      // DIFF & COMPARISON
      // ============================================
      "Bash(diff *)",         // File comparison
      "Bash(cmp *)",          // Binary comparison
      "Bash(comm *)",         // Compare sorted files
      
      // ============================================
      // MCP (MODEL CONTEXT PROTOCOL) TOOLS
      // ============================================
      "mcp__ide__getDiagnostics",  // Get IDE diagnostics (linting, type errors, warnings)
      
      // ============================================
      // CLAUDE CODE SKILLS
      // ============================================
      "Skill(research-assessor)",
      "Skill(skill-creator)",
      "Skill(example-skills:skill-creator)"
    ],
    
    // ============================================
    // DENIED OPERATIONS (SAFETY)
    // ============================================
    "deny": [
      // Destructive file operations
      "Bash(rm -rf *)",
      "Bash(rm -r *)",
      
      // Privilege escalation
      "Bash(sudo *)",
      "Bash(su *)",
      
      // Dangerous git operations
      "Bash(git push origin --force)",
      "Bash(git push --force *)",
      "Bash(git push -f *)",
      "Bash(git reset --hard *)",
      "Bash(git clean -fd *)",
      
      // System file access
      "Read(/etc/*)",
      "Read(/root/*)",
      
      // SSH & credentials
      "Read(/home/*/.ssh/*)",
      "Read(**/.env)",
      "Read(**/secrets/**)",
      
      // System modifications
      "Bash(reboot)",
      "Bash(shutdown *)",
      "Bash(halt)",
      "Bash(poweroff)",
      
      // Package system modifications
      "Bash(apt *)",
      "Bash(yum *)",
      "Bash(dnf *)",
      
      // Disk operations
      "Bash(dd *)",
      "Bash(mkfs *)",
      "Bash(fdisk *)",
      
      // Network modifications
      "Bash(iptables *)",
      "Bash(ifconfig *)",
      "Bash(ip addr *)",
      
      // Process management (system-wide)
      "Bash(killall *)",
      "Bash(pkill *)",
      
      // Cron/scheduled tasks
      "Bash(crontab *)",
      "Bash(at *)",
      
      // Service management
      "Bash(systemctl *)",
      "Bash(service *)"
    ],
    
    "ask": []
  },
  
  // ============================================
  // ENVIRONMENT CONFIGURATION
  // ============================================
  "env": {
    "CLAUDE_CODE_MAX_OUTPUT_TOKENS": "64000"
  }
}